package com.mineinabyss.geary.ecs

/*
@Serializable
abstract class GearyComponent( //TODO name better
        open val cooldown: Int = 0,
        val conditions: List<String> = listOf()
) : GearyComponent() {

    @Transient //TODO might need to serialize this later
    private var cooldownStart: Int = 0

    val cooledDown get() = Bukkit.getServer().currentTick - cooldownStart > cooldown
    fun restartCooldown() {
        cooldownStart = Bukkit.getServer().currentTick
    }
}*/
