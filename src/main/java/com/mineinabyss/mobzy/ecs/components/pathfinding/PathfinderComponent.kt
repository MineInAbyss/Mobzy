package com.mineinabyss.mobzy.ecs.components.pathfinding

/*
@Serializable
class PathfinderComponent<T : AnyCustomMob>(val pathfinders: Map<Int, MobzyPathfinder<T>>): MobzyComponent<T>() {
    override fun applyTo(customMob: T) {
        pathfinders.forEach { (priority, pathfinder) ->
            customMob.nmsEntity.addPathfinderGoal(priority, pathfinder.build(customMob))
        }
    }
}*/
