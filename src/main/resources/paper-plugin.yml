name: Mobzy
version: "${plugin_version}"
author: Offz
main: com.mineinabyss.mobzy.MobzyPlugin
api-version: "1.20"
prefix: Mobzy
description: A custom entities API

dependencies:
  server:
    Geary:
      required: true
      load: BEFORE
      join-classpath: true
    ModelEngine:
      required: false
      load: BEFORE
      join-classpath: true
    WorldGuard:
      required: false
      load: BEFORE
      join-classpath: true
