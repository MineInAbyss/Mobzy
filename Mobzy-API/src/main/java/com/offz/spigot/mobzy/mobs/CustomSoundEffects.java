package com.offz.spigot.mobzy.mobs;

public class CustomSoundEffects {
    /*public static final SoundEffect CORPSE_WEEPER_HURT = create("entity.corpseweeper.hit");

    private static SoundEffect create(String path) {
        MinecraftKey key = new MinecraftKey(path);
        SoundEffect effect = SoundEffect.a(key, new SoundEffect(key));
        if (effect == null)
            throw new IllegalStateException("Invalid Sound requested: " + path);
        else
            return effect;
    }*/
}
