rootProject.name = 'mobzy'

if (rootProject.hasProperty("useLocalGuiy") && useLocalGuiy.toBoolean()) includeBuild '../guiy'

if (rootProject.hasProperty("useLocalIdofront") && useLocalIdofront.toBoolean()) includeBuild '../idofront'